# Desafio Bry

Para rodar a aplicação, execute:
```bash

mvn spring-boot:run
```
ou
```bash

mvn clean package
java -jar target/desafio-bry-{versão}.jar
```

Para rodar os testes, execute:
```bash

mvn test

```